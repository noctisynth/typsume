{
  "name": "@typsume/cli",
  "version": "0.0.0",
  "type": "module",
  "description": "Data-driven Typst resume compiler — build resume.json/yaml/toml → PDF via WASM (no system typst needed).",
  "license": "MIT",
  "bin": "./src/main.ts",
  "scripts": {
    "typsume": "bun run src/main.ts"
  },
  "dependencies": {
    "@iarna/toml": "^2.2.5",
    "@myriaddreamin/typst.ts": "^0.7.0",
    "@typsume/core": "workspace:*",
    "citty": "^0.1.6",
    "fflate": "^0.8.3",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/js-yaml": "^4.0.9",
    "typescript": "^5.9.3"
  },
  "engines": {
    "bun": ">=1.4.0",
    "node": ">=20.0.0"
  }
}
