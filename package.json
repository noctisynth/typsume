{
  "name": "@typsume/workspace",
  "private": true,
  "type": "module",
  "description": "Data-driven Typst resume compiler. CLI + Web share one schema and one WASM compiler.",
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "templates/*"
  ],
  "scripts": {
    "check": "biome check .",
    "fix": "biome check --write .",
    "format": "biome format --write .",
    "typsume": "bun run packages/cli/src/main.ts",
    "test": "bun run -F '*' test",
    "test:core": "bun run -F @typsume/core test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "@myriaddreamin/typst-ts-renderer": "^0.7.0",
    "@myriaddreamin/typst-ts-web-compiler": "^0.7.0",
    "@myriaddreamin/typst.ts": "^0.7.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "bun": ">=1.4.0",
    "node": ">=20.0.0"
  },
  "packageManager": "bun@1.4.0"
}
